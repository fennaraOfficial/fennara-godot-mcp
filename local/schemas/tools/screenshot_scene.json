{
  "name": "screenshot_scene",
  "description": "Open a scene file and capture a PNG image. Use camera_path to render from a specific Camera2D or Camera3D in the scene through a temporary SubViewport when authored camera framing matters. For 2D scenes, optionally specify a world-coordinate view_rect for editor viewport capture; camera_path overrides view_rect and uses the Camera2D view. For 3D scenes, optionally specify view (front, back, left, right, top, perspective, or all) and target_node_path to frame a specific node/subtree in the editor viewport with surrounding context; camera_path overrides view/target framing and uses the Camera3D view. Defaults to perspective. Internally the primary image is image_base64: for one view it is that view, for multi-view it is the full-resolution no-downscale labeled collage. Every generated screenshot is saved under user://.fennara/screenshots and returned as image_res_path plus cross-platform absolute image_path. In local MCP responses, base64 is stripped; use the returned image_path values, or read the image later with a file/image-capable tool. Multi-view responses include per-view image_path entries.",
  "parameters": {
    "type": "object",
    "properties": {
      "scene_path": {
        "type": "string",
        "description": "Path to the scene file to open and capture (e.g. 'res://scenes/level1.tscn')"
      },
      "view_rect": {
        "type": "object",
        "description": "2D editor-viewport capture only. World-coordinate bounding box to frame. Omit to use the project's viewport resolution from Project Settings as the default view area. Ignored when camera_path is provided.",
        "properties": {
          "x1": { "type": "number", "description": "Left edge X coordinate" },
          "y1": { "type": "number", "description": "Top edge Y coordinate" },
          "x2": { "type": "number", "description": "Right edge X coordinate" },
          "y2": { "type": "number", "description": "Bottom edge Y coordinate" }
        },
        "required": ["x1", "y1", "x2", "y2"]
      },
      "camera_path": {
        "type": "string",
        "description": "Optional node path relative to the scene root for a Camera2D or Camera3D to capture from. The tool instantiates the scene into a temporary SubViewport, makes that camera current, captures the viewport texture, then removes the temporary viewport. This is an authored scene-camera render, not a runtime `_ready()`/Play-mode capture."
      },
      "view": {
        "type": "string",
        "description": "3D editor-viewport capture only. Camera view to capture: front, back, left, right, top, perspective, isometric, or all. Defaults to perspective. Ignored when camera_path is provided.",
        "enum": ["front", "back", "left", "right", "top", "perspective", "isometric", "all"]
      },
      "target_node_path": {
        "type": "string",
        "description": "3D only. Optional node path relative to the scene root. Frames this node/subtree instead of the whole scene. Choose the smallest meaningful visual subtree: if you target a very large node such as a floor, terrain, or whole level section, the tool must fit all of that node's bounds in frame and smaller objects on/near it will appear tiny."
      },
      "context_margin": {
        "type": "number",
        "description": "3D only. Multiplier applied to the framed bounds before positioning the camera. Smaller values zoom closer; larger values show more surrounding context. Defaults to 0.75 for target_node_path captures and 1.1 for whole-scene captures. Values below 1.0 are valid for close inspection."
      }
    },
    "required": ["scene_path"]
  }
}
